import React from 'react'

const userContext = React.createContext()

export default userContext ; 
// this is user provider ..... ye sare components sari states ka access le skte h 
// mmake a user context provider 
